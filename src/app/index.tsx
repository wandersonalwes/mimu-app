import { Text, View } from 'react-native'

export default function HomeScreen() {
  return (
    <View style={{ flex: 1 }}>
      <Text style={{ fontSize: 24, fontFamily: 'ManropeBold' }}>Mimu</Text>
    </View>
  )
}
